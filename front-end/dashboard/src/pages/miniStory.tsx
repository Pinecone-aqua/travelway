// import { useEffect, useState } from "react";

export default function MiniStory(props: { id: string }): JSX.Element {
  const id = props.id;
  //const [miniStory, setMiniStory] = useState();
  console.log(id);

  //   useEffect(() => {
  //     fetch(`http://localhost:3009/quests/${id}`)
  //       .then((reponse) => response.json())
  //       .then((res) => setMiniStory(res));
  //   }, []);
  return <>aaaa</>;
}
